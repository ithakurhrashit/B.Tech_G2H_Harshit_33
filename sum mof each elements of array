//WAP that inputs two array from user and saves the sum of corresponding elements 
// in thoird array and print the.
#include<stdio.h>
int main(){
    int a , b ,c ;
    printf("enter the elements of first and second  ");
    scanf(" %d %d  ", &a , &b );
    int first[a],second[b] , sum[100];
    for(int i=0; i<a;i++){
        printf("enter %d element of array :",i);
        scanf(" %d ", &first[i]);
    }
    for(int i=0; i<b;i++){
        printf("enter %d element of array :",i);
        scanf(" %d ", &second[i]);
    }
    for(int i=0;i<b;i++){
        sum[i]=first[i]+second[i];
        printf(" thesum of %d element of both array is %d",i, sum[i]);
    }
    return 0 ;
    
    
}
